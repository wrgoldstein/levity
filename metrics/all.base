base "orders" {
  view = "orders"

  join "customers" {
    sql = "customers.customer_id = orders.customer_id"
  }
}

base "customers" {
  view = "customers"

  join "orders" {
    sql = "customers.customer_id = orders.customer_id"
  }
}
